describe('addition', () => {
  it('1 + 1 must be 2', () => {
    expect(1 + 1).toBe(2);
  });
});

describe('subtraction', () => {
  it('1 - 1 must be 0', () => {
    expect(1 - 1).toBe(0);
  });
});

describe('multiplication', () => {
  it('1 * 3 must be 3', () => {
    expect(1 * 3).toBe(3);
  });

  it('2 * 3 must be 6', () => {
    expect(2 * 3).toBe(6);
  });
});
